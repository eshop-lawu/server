<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lawu.eshop.mall.srv.mapper.extend.MessageDOMMapperExtend" >
    <resultMap id="BaseResultMap" type="com.lawu.eshop.mall.srv.domain.extend.MessageDOView" >
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <id column="id" property="id" jdbcType="BIGINT" />
        <result column="user_num" property="userNum" jdbcType="VARCHAR" />
        <result column="relate_id" property="relateId" jdbcType="BIGINT" />
        <result column="type" property="type" jdbcType="TINYINT" />
        <result column="content" property="content" jdbcType="VARCHAR" />
        <result column="status" property="status" jdbcType="TINYINT" />
        <result column="gmt_modified" property="gmtModified" jdbcType="TIMESTAMP" />
        <result column="gmt_create" property="gmtCreate" jdbcType="TIMESTAMP" />
        <result column="title" property="title" jdbcType="VARCHAR" />

    </resultMap>

    <!-- 根据用户编号查询站内信息列表-->
    <select id="selectByUserNum" resultMap="BaseResultMap" parameterType="com.lawu.eshop.mall.param.MessageQueryParam" >
        select
        m.id,
        m.user_num,
        m.relate_id,
        m.type,
        m.content,
        m.status,
        mt.title
        from message m
        INNER JOIN message_template mt
        ON  m.type = mt.type
        where m.user_num = #{userNum}
        AND m.status &lt;&gt; 2
        ORDER  BY
        m.id DESC
        LIMIT #{offset},#{pageSize}
    </select>

    <!-- 根据用户编号查询站内信息总数-->
    <select id="selectCountByUserNum" resultType="java.lang.Integer" parameterType="java.lang.String" >
        select
        count(*)
        from message m
        INNER JOIN message_template mt
        ON  m.type = mt.type
        where m.user_num = #{userNum}
        AND m.status &lt;&gt; 2
    </select>

</mapper>