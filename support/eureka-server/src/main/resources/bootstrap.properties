spring.application.name=eureka-server
spring.profiles.active=local

logging.path=${user.home}/logs/eureka-server

# 关闭自我保护模式(默认开启)
eureka.server.enable-self-preservation=false
# 设置清理无效节点的时间间隔
eureka.server.eviction-interval-timer-in-ms=10000